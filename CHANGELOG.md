# 版本歷史

本檔案記載專案對外釋出之版本變更。版本號見 `manifest.json` 的 `version` 欄位。

## 3.0 — 2026-05-19

本版新增「**租金不當得利**」模組，並全面重構分頁列以容納第六個模組；屬於功能性重大改版，故主版號自 2.x 進至 3.0。

### 新增

- **租金不當得利試算**（新分頁）：
  - 依占用期間（起迄日均計入）與年息（1%–10% 滑桿）試算相當於租金之不當得利。
  - 支援**多筆土地**輸入，每筆可分別填入地號、占用面積（㎡）及**逐年度申報地價**；提供「同上」按鈕一鍵複製上一年度地價。
  - 可加計**房屋現值**作為基數（選填）。
  - 占用期間**跨年自動分段**，每段以該年度地價計算 `(土地申報總價 + 房屋現值) × 年息 × (天數 / 365)`，元以下四捨五入後彙總。
  - 提供「**由迄日回推 5 年**」快速鈕（對應消滅時效）。
  - 自動產出段落式**書狀草稿**，含期間、各年度地價、計算式與合計。
  - 提供「**帶入裁判費**」按鈕，可將總請求金額一鍵寫入速算分頁之標的金額。
- **利息計算**：每段計息期間新增「**帶入本金**」按鈕，可將該群組之請求本金一鍵填入計息本金欄位，便於常見「計息本金 = 請求本金」之輸入。

### 變更

- 擴充功能名稱維持「臺灣民事實務工具」，`manifest.json` 描述新增「租金不當得利試算」。
- **分頁列重構**：改為雙列 pill switch 設計（第一列：速算．裁判費／救濟期間／日期計算；第二列：利息計算／折舊計算／租金不當得利），thumb 寬度依分頁數量自動計算（CSS `--n` 變數），未來新增分頁不需另寫樣式。
- **折舊計算**：出廠年月輸入更寬鬆，支援 `YYYY`、`YYYYMM`、`YYYYMMDD` 三種長度（不足者自動補月、日）。

### 維護

- 抽出共用工具函式 `digitFilter`（純數字輸入過濾與長度截斷）與 `attachCalendar`（文字輸入框與原生 `<input type="date">` 同步），各分頁日期／數字輸入統一使用，減少重複程式碼。
- `TabManager` 改以掃描 DOM 中所有 `.tab-switch` 容器自動初始化，並支援 `registerActivateHook` 註冊切換時的延遲綁定（避免大型分頁初始時就建構 DOM）。
- 折舊、利息、不當得利等分頁採延遲綁定（首次點擊該分頁時才 `bind()`），加快擴充功能啟動。

---

## 2.1 — 2026-05-18

### 變更

- 擴充功能正式名稱改為 **臺灣民事實務工具**（原：民事速算機）。
- 重寫 `README.md`，補齊五個功能模組說明、安裝步驟與法規摘要。

### 修正

- 利息計算分頁之計息起迄日改為支援 **8 碼**輸入（與其他分頁一致，可輸入西元 8 碼日期）。
- 利息計算各期完成試算後，於期間列顯示 **折算年數與該期利息金額**。
- 救濟期間「就審天數」切換器初始狀態與標籤對齊（5 日／10 日）。

### 維護

- 移除未使用之日期格式化函式。
- 利息試算動態 HTML 輸入值加上跳脫處理；複製至剪貼簿失敗時不再拋出未處理錯誤。

---

## 2.0 — 2026-05-13

### 新增

- 以 **MIT License** 公開授權（`LICENSE`）。
- 本版本歷史檔案（`CHANGELOG.md`）。

### 變更

- 擴充套件版本號由 **1.4** 調整為 **2.0**，標示首次採 MIT **公開釋出**之里程碑；**功能與運算邏輯與 1.4 相同**，未另作行為變更。

---

## 1.4 —（先前釋出）

- Manifest V3 擴充套件（當時名稱「民事速算機」）：速算與裁判費試算（含新舊制費率）、救濟期間試算（在途、送達、假日順延）、日期區間／推算／年齡、遲延利息、車輛修復費折舊、彈出視窗與側邊面板、假日資料經 jsDelivr 載入等。詳見 `README.md`。
